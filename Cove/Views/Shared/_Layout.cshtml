<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">

    @*<link href="https://cdnjs.cloudflare.com/ajax/libs/chosen/1.8.7/chosen.css" rel="stylesheet" id="chosen-css">*@
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
    @*<script src="https://cdnjs.cloudflare.com/ajax/libs/chosen/1.8.7/chosen.jquery.min.js"></script>*@
    <script src="~/lib/jquery-validation/dist/jquery.validate.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.js"></script>
    <script src="~/lib/chosen/chosen.proto.js"></script>
    <script src="~/lib/chosen/chosen.jquery.js"></script>
    <link rel="stylesheet" href="~/common/css/style.css" />

    <style>
        body {
            font-family: "Lato",WorkSans, sans-serif;
        }

        /* Fixed sidenav, full height */
        .sidenav {
            height: 100%;
            width: 200px;
            position: fixed;
            z-index: 1;
            top: 0;
            left: 0;
            background-color: #2c34aa;
            overflow-x: hidden;
            padding-top: 20px;
            /*            margin-top: 84px;*/
        }

            /* Style the sidenav links and the dropdown button */
            .sidenav a, .dropdown-btn {
                padding: 6px 8px 6px 16px;
                text-decoration: none;
                display: block;
                border: none;
                background: none;
                width: 100%;
                text-align: left;
                cursor: pointer;
                outline: none;
                opacity: 0.8;
                font-family: WorkSans;
                color: #ffffff;
            }

                /* On mouse-over */
                .sidenav a:hover, .dropdown-btn:hover {
                    color: #ffffff;
                }

        /* Main content */
        .main {
            margin-left: 200px; /* Same as the width of the sidenav */
            font-size: 14px; /* Increased text to enable scrolling */
            padding: 0px 10px;
            background-color: #fcfcff;
            /*            margin-top: 84px;*/
        }

        /* Dropdown container (hidden by default). Optional: add a lighter background color and some left padding to change the design of the dropdown content */
        .dropdown-container {
            display: none;
            background-color: darkblue;
            padding-left: 10px;
        }

        /* Optional: Style the caret down icon */
        .fa-caret-down {
            float: right;
            padding-right: 8px;
        }

        .sidenav-head {
            display: inline-flex;
        }

        /* Some media queries for responsiveness */
    </style>
</head>
<body>
    @*<div class="sidenav-head">
        <div class="facebook"></div>
        <div class="sidenav-heading"><img />Manage Account</div>
        <div class="sidenav-right">AB Adam Baston dropdown notification-bell</div>
    </div>*@
    <div class="sidenav">

        @Html.ActionLink("Home", "Welcome", "Account")

        <button class="dropdown-btn" role="menu">Comics<i class="fa fa-caret-down"></i></button>
        <div class="dropdown-container">
            @Html.ActionLink("Upload Comic", "UploadComic", "UploadComic", null, new { id = "" })
        </div>
        @Html.ActionLink("Collaboration", "Collaboration", "UploadComic")
        @Html.ActionLink("Chat", "Collaboration", "UploadComic")
        @Html.ActionLink("Manage Account", "ManageAccountOptions", "ManageAccount")
        @Html.ActionLink("Log Out", "Logout", "Account")

    </div>

    <div class="main">
        @RenderBody()

    </div>
    @RenderSection("Scripts", required: false)

    <script>
        /* Loop through all dropdown buttons to toggle between hiding and showing its dropdown content - This allows the user to have multiple dropdowns without any conflict */
        var dropdown = document.getElementsByClassName("dropdown-btn");
        var i;

        for (i = 0; i < dropdown.length; i++) {
            dropdown[i].addEventListener("click", function () {
                this.classList.toggle("active");
                var dropdownContent = this.nextElementSibling;
                if (dropdownContent.style.display === "block") {
                    dropdownContent.style.display = "none";
                } else {
                    dropdownContent.style.display = "block";
                }
            });
        }
    </script>

</body>
</html>